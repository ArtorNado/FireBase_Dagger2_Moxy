<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.homework.fbmxdg">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".containerForNui.ContainerNuiActivity"
            android:label="@string/title_activity_container_nui"
            android:theme="@style/AppTheme"></activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7102171154252511~2952592786" />

        <activity
            android:name=".containerActivity.ContainerActivity"
            android:label="@string/title_activity_container"
            android:theme="@style/AppTheme" />
        <activity android:name=".passwordLess.PasswordLessActivity" />
        <activity android:name=".registration.RegistrationActivity" />
        <activity android:name=".signIn.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
